/**
 * _isdigit- to check the character is digit or not
 *
 * @c: the character is given
 *
 * retutn: 0 is succsuss
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return 0;
}
